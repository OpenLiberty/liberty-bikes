export class Player {
  public name: string;
  public status: string;
  public color: string;

  constructor(name: string, status: string, color: string) {
    this.name = name;
    this.status = status;
    this.color = color;
  }
}
